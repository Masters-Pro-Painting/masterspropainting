# Official badge artwork

Drop official files here with these exact names. The next build swaps them in for the text badges.

| File | Where to get it |
|---|---|
| `google-verified.svg` / `.png` / `.webp` | Google Local Services Ads profile |
| `pca.png` | Already added: official PCA logo |

Badge links live in `PROFILES` in `src/data/site.ts`.
